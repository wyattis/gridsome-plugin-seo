
describe('extract', () => {
  it('should work with strings', () => {
    const data = {
      path: 'wowza',
      keywords: 'nope',
      description: 'okay'
    }
    const config = {
      extract: {
        default: {
          
        }
      }
    }
    expect(extract())
  })
})